trigger LoyTxt on CVL__Loy_Transaction__c (before insert,before update) {
/*    if(Trigger.isBefore)
    {
        if(Trigger.isInsert || Trigger.isUpdate)
        {
			SM012_Voucher.updateVisits();
        }
    }*/
}